name: datafusion
description: A new Flutter application.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  linalg: ^0.3.2
  fast_noise: ^0.1.3+1
  rxdart: ^0.24.0
  animations: ^1.0.0+5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true